// import { getVoidLogger } from '@backstage/backend-common';
// import express from 'express';
// import request from 'supertest';

// import PillarAdoptionService from './PillarAdoptionService';

// jest.mock('@backstage/catalog-client', () => {
//   return function () {
//     return {
//       CatalogClient: jest.fn(),
//     };
//   };
// });

// import {
//   CatalogClient,
//   CATALOG_FILTER_EXISTS,
// } from '@backstage/catalog-client';

// describe('PillarAdoptionService', () => {
//   let app: express.Express;
//   let pillarAdoptionService: PillarAdoptionService;
//   let catalogClient: CatalogClient;

//   beforeAll(async () => {
//     catalogClient = CatalogClient.mockImplementation(() => ({
//       messages: () => ({
//         send: send,
//       }),
//     }));
//     app = express().use(router);
//   });

//   beforeEach(() => {
//     jest.resetAllMocks();
//   });

//   describe('GET /health', () => {
//     it('returns ok', async () => {
//       const response = await request(app).get('/health');

//       expect(response.status).toEqual(200);
//       expect(response.body).toEqual({ status: 'ok' });
//     });
//   });

//   describe('GET /fetch', () => {
//     it('returns the resource from github', async () => {
//       const response = await request(app)
//         .get('/riskive/test')
//         .query({ path: 'foo/bar', branch: 'feat/x1' });

//       expect(response.text).toEqual('hello folks');
//       expect(response.status).toEqual(200);
//     });
//   });
// });
